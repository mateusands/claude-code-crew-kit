{
  "name": "claude-code-crew-kit",
  "private": true,
  "description": "Development scripts for the kit itself. install.sh does not copy this into a consuming project.",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  }
}
